const { updateWorkflow } = require('./src/actions/workflows.js'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const ws = await prisma.workflow.findMany({ select: { id: true, nodesJson: true }}); for (const w of ws) { await prisma.workflow.update({ where: { id: w.id }, data: { nodesJson: w.nodesJson } }); console.log('Updated', w.id); } console.log('Done'); } run().catch(console.error).finally(() => prisma['$disconnect']());
