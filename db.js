const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const notifs = await prisma.notification.findMany({ where: { status: 'UNREAD' } }); console.log('UNREAD:', JSON.stringify(notifs, null, 2)); const wfs = await prisma.workflow.findMany({ select: { id: true, name: true, nodesJson: true } }); console.log('Workflows:', JSON.stringify(wfs.map(w => ({id: w.id, name: w.name, nodes: w.nodesJson ? JSON.parse(w.nodesJson).length : 0})), null, 2)); } main().finally(() => prisma.$disconnect());
